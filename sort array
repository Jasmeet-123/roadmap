def sortArr(self, arr, n): 
        #code here
        #for i in range(0,n):
         #   for j in range(i+1,n):
              #  if arr[i]>arr[j]:
                    #temp = arr[i]    
                    #arr[i] = arr[j]   
                    #arr[j] = temp
                  #  arr[i],arr[j]=arr[j],arr[i]
        arr.sort()
        return arr
